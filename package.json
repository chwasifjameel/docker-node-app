{
  "name": "ls",
  "version": "1.0.0",
  "description": "a docker node app to learn how to setup docker in a nodejs app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
